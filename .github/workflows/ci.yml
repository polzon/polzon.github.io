name: CI/CD Pipeline

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml

  deploy:
    needs: e2e
    uses: ./.github/workflows/deploy.yml
