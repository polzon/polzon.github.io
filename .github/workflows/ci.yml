name: CI/CD Pipeline

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  e2e:
    uses: ./.github/workflows/e2e.yml

  deploy:
    needs: e2e
    uses: ./.github/workflows/deploy.yml
